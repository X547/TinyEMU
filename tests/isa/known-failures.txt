# Tests that are expected to fail, so that run.sh can be used as a regression
# gate: a test listed here that fails is reported but does not fail the run, and
# one that starts passing is reported as an unexpected pass so the entry can be
# removed.
#
# Format: <test name> <whitespace> <reason>

rv64ua-p-lrsc		sc does not clear the reservation, so a second sc to the same address succeeds (riscv_cpu_template.h, case 3)
rv64ud-p-move		fmv.d.x / fmv.x.d NaN boxing
rv64mi-p-breakpoint	debug triggers (tdata1/tdata2) are not implemented
rv64mi-p-pmpaddr	PMP is not implemented
rv64mi-p-instret_overflow	minstret does not wrap as the test expects
